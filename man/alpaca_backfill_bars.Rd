% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{alpaca_backfill_bars}
\alias{alpaca_backfill_bars}
\title{Backfill Historical Bar Data}
\usage{
alpaca_backfill_bars(
  symbols,
  timeframes = "1Day",
  start,
  end = Sys.time(),
  path = "alpaca_bars.csv",
  adjustment = "raw",
  feed = NULL,
  sleep = 0.25,
  keys = get_api_keys(),
  data_base_url = get_data_base_url()
)
}
\arguments{
\item{symbols}{Character vector; ticker symbols (e.g., \code{c("AAPL", "MSFT")}).}

\item{timeframes}{Character vector; bar timeframes (e.g., \code{c("1Day", "1Hour")}).
See \code{alpaca_timeframe_map} for valid values.}

\item{start}{Character or POSIXct; start date/time.}

\item{end}{Character or POSIXct; end date/time. Defaults to \code{Sys.time()}.}

\item{path}{Character; file path for CSV output. Default \code{"alpaca_bars.csv"}.}

\item{adjustment}{Character; price adjustment type. Default \code{"raw"}.}

\item{feed}{Character or NULL; data feed (\code{"iex"} or \code{"sip"}).}

\item{sleep}{Numeric; seconds to pause between requests (rate limiting).
Default \code{0.25}.}

\item{keys}{List; API credentials. Defaults to \code{\link[=get_api_keys]{get_api_keys()}}.}

\item{data_base_url}{Character; market data base URL. Defaults to \code{\link[=get_data_base_url]{get_data_base_url()}}.}
}
\value{
\code{data.table} with all downloaded data (invisibly). Also writes to CSV.
Has a \code{"failures"} attribute listing any symbol-timeframe combos that failed.
}
\description{
Downloads historical OHLCV bar data for one or more symbols and timeframes,
saving results to a CSV file. Supports resuming interrupted downloads by
reading existing data and skipping completed symbol-timeframe combinations.
}
\examples{
\dontrun{
# Download daily bars for AAPL and MSFT
dt <- alpaca_backfill_bars(
  symbols = c("AAPL", "MSFT"),
  timeframes = "1Day",
  start = "2020-01-01",
  path = "data/bars.csv"
)

# Resume an interrupted download
dt <- alpaca_backfill_bars(
  symbols = c("AAPL", "MSFT", "TSLA"),
  timeframes = c("1Day", "1Hour"),
  start = "2020-01-01",
  path = "data/bars.csv"
)
}

}
