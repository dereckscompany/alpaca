% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types_alpaca.R
\name{alpaca_shapes}
\alias{alpaca_shapes}
\title{Alpaca return shapes}
\description{
Reusable roxyassert \verb{@type} shapes for the \code{data.table}s returned
by the Alpaca R6 client methods (\link{AlpacaMarketData}, \link{AlpacaTrading},
\link{AlpacaAccount}, \link{AlpacaOptions}). Each public method documents its return as
\verb{Shape | promise<Shape>}; the contract roclet expands the shape into the
generated \verb{assert_return_*} helper, which checks that every listed column is
present and of its column type. \code{assert_has_columns} requires the listed
columns but tolerates EXTRA ones, so each shape names only the columns Alpaca
guarantees on every response that flows to that contract; venue-optional
columns (e.g. the flattened \verb{admin_configurations_*} account fields, the
options \verb{greeks_*} / \code{implied_volatility} columns, the richer single-order
price fields) ride along as un-asserted extras.

Column-type conventions, matched to what the parsers actually emit (verified
against the \code{tests/testthat} mock fixtures): \code{character} for the
string-typed price/quantity fields Alpaca returns as JSON strings (the API
never narrows these to numbers, so neither do we); \code{integer} for the bar
\code{volume} / \code{trade_count} and the trade/quote \code{size} fields; \code{numeric} (strict
double) for true floating-point prices; \code{POSIXct} for parsed timestamps and
\code{Date} for calendar dates. A column is marked \verb{| NA} only where a value can
legitimately be missing on a present row. The whole-number-double screener
counters (\code{volume} / \code{trade_count} on \code{MostActives}) are typed
\code{integer | numeric} because Alpaca encodes them without a decimal and the
JSON parser realises them as either depending on magnitude.

\verb{@genassert} emits a standalone \verb{assert_type_<Shape>()} for each shape;
\verb{@exportassert} exports them so downstream packages can validate against
Alpaca's shapes.
}
